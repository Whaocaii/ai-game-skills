# 密钥管理

本仓库不分发 API key、令牌、私钥或个人配置。所有配套测试和本地示例无需凭据。

接入真实模型时，浏览器调用项目自己的服务端接口；服务端从环境变量或密钥管理服务读取凭据。不要把密钥放进前端代码、URL、提示词、截图、测试数据或日志。不要复制本机 `.env` 文件到 Skill 包中。

`.gitignore` 只能阻止部分未跟踪文件被加入，不能清理已有历史。提交前使用 Gitleaks 扫描工作文件与完整 Git 历史；扫描报告使用 `--redact`，保存在仓库外。

```bash
gitleaks dir . --redact --no-banner
gitleaks git . --log-opts="--all" --redact --no-banner
```

若发现真实密钥，先停止发布，撤销或轮换凭据，再清理文件与历史。扫描无命中说明工具在本次范围内未发现密钥，不等于任何检测工具能够识别所有自定义凭据。
