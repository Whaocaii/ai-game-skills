# 维护与贡献

本库围绕真实的 AI 游戏设计、开发与评估任务演进。优先修复一个可复现的问题，再决定是否新增模块。

## 改进现有 Skill

1. 描述触发问题的用户请求、项目约束和当前结果。
2. 指出流程在哪个判断点缺少信息、产生歧义或给出错误行动。
3. 修改该判断与交付标准，保留未受影响的任务范围。
4. 用原始案例复核结果，记录模型、环境、实际运行范围和未验证部分。
5. 更新 `CHANGELOG.md`；行为或依赖变化时同步目录和说明。

不要仅通过增加“必须”“严禁”来替代实际原因，也不要把一个案例的参数变成所有项目的默认规则。

## 新增 Skill

- 使用 `skills/<英文短横线名称>/SKILL.md`，名称与前置元数据一致。
- 写明输入、关键判断、产出与失败处理；提供一个有具体约束的使用案例。
- 判断是否与已有模块重叠，优先组合现有模块而非复制整套流程。
- 注册到 `catalog.json`。若总调度器需要使用它，同时更新总调度的路由表和安装依赖。
- 只有执行任务需要时才增加参考资料、脚本和模板；脚本需有实际输入输出测试。
- 更新 README 目录及数量；说明新增依赖与实际支持的工具环境。
- 引入第三方内容时，核对具体许可并保留来源。

## 本地检查

```bash
python3 scripts/validate.py
python3 -m unittest discover -s tests -v
python3 scripts/install.py all --dry-run
```

安装到临时目录检验依赖，不覆盖自己正在使用的 Skill。需要真实模型、引擎、图片服务的验证另行执行，不能用结构检查代替。

## 版本

`0.x` 为早期迭代。版本号在 `catalog.json`、README 与 CHANGELOG 中保持一致；单个模块的 `metadata.revision` 表示其最近行为版本。发布说明列出新增、变化、迁移方式与实际验证。

PR 描述应包含具体问题、改后行为和验证结果。避免把结构调整或措辞变化写成未经测试的能力提升。

## 检索信息维护

新增或调整 Skill 时，同步中英文 README 和 `catalog.json` 的英文标题、描述、关键词及入口路径。描述写清能力与触发场景；关键词使用真实术语和常见同义词，不添加无关品牌、未经验证的兼容性或排名承诺。安装依赖与原有模块名称保持稳定。
